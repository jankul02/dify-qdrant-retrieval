# Claude-Specific Rules

- `CLAUDE.md` is the entry point — read it first, every session.
- Session branches: `session/YYYY-MM-DD-short-slug`.
- Use Read/Edit tools for files — avoid Bash cat/echo.
- Do not push to remote without explicit developer confirmation.
- Save cross-session insights to `.claude/` memory.
- Symbol-first: search `.tags` or grep before reading source files.
- When spawning subagents: include full context — they start cold.
